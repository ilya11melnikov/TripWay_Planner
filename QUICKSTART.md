# 🚀 Быстрый старт TripWay Planner

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка API ключей (опционально)

Создайте файл `.env` в корне проекта:

```env
REACT_APP_OPENTRIP_API_KEY=your_key_here
REACT_APP_GOOGLE_MAPS_API_KEY=your_key_here
```

**Примечание:** Проект будет работать и без этого - используются демо-ключи.

## Шаг 3: Запуск проекта

```bash
npm start
```

Приложение откроется на [http://localhost:3000](http://localhost:3000)

## Шаг 4: Использование

1. **Главная страница** - Просмотрите рекомендации и выберите категорию
2. **Поиск** - Введите название города или места
3. **Детали** - Кликните на карточку, чтобы увидеть подробности
4. **План путешествия** - Добавляйте места в свой маршрут и перетаскивайте между днями

## 🛠️ Доступные команды

- `npm start` - Запуск dev-сервера
- `npm run build` - Сборка для production
- `npm test` - Запуск тестов

## 📝 Примечания

- Данные сохраняются в LocalStorage браузера
- Переключение темы сохраняется автоматически
- Drag & Drop работает на странице плана путешествия

## 🐛 Решение проблем

### Ошибки при установке
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портом
Закройте порт 3000 или используйте другую переменную окружения:
```bash
PORT=3001 npm start
```

